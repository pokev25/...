[include]
    path = .gitconfig.local

[alias]
    serv = daemon --reuseaddr --base-path=. --export-all --verbose

[core]
    editor = vim
    excludesfile = ~/.gitignore.global

[color]
    ui = true
[push]
    default = simple
